#ifndef SPACE_COMPONENTS_FADE_HPP
#define SPACE_COMPONENTS_FADE_HPP

namespace space::components {

struct Fade {
    float total = 1.0f;
    float current = 0.0f;
};

}  // namespace space::components

#endif
